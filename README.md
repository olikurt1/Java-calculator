Setting up a java project creates libraries and binaries folders along with .vscode configurations for project. 

To run a program, such as an app, the java class recursively calls itself. all frame methods are used to create the window for the calculator

Common java theme, when creating a variable/value from an imported library, typical syntax is: 
dataType variable name = new dataType value; This is a generic blueprint variable which further variables of such data type may need when being applied to use. 